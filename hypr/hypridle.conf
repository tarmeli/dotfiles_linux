general {
    lock_cmd = node ~/.config/hypr/scripts/lockscreen.js          # dbus/sysd lock command (loginctl lock-session)
    unlock_cmd = notify-send "unlock!"      # same as above, but unlock
    before_sleep_cmd = notify-send "Zzz"    # command ran before sleep
    after_sleep_cmd = notify-send "Awake!"  # command ran after sleep
    ignore_dbus_inhibit = false             # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
    ignore_systemd_inhibit = false          # whether to ignore systemd-inhibit --what=idle inhibitors
}

listener {
    timeout = 180                           # in seconds
    on-timeout = notify-send "Idle!" && brightnessctl --device=intel_backlight s 1% # command to run when timeout has passed
    on-resume = notify-send "Welcome back!" && brightnessctl --device=intel_backlight s 40%  # command to run when activity is detected after timeout has fired.
}

listener {
    timeout = 300
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 1200
    on-timeout = systemctl suspend
}
